<ui:FluentWindow x:Class="RetroRommer.Core.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:RetroRommer.Core"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="RetroRommer" Height="700" Width="700" WindowStartupLocation="CenterScreen" MinHeight="500" MinWidth="650">

    <ui:FluentWindow.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </ui:FluentWindow.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="RetroRommer" Grid.Row="0"/>

        <!-- Files Section -->
        <GroupBox Header="Files &amp; Paths" Grid.Row="1" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="140"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Content="MAME Report File:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <ui:TextBox x:Name="TextBoxFilename" Grid.Row="0" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>
                <ui:Button x:Name="ButtonSelectFile" Grid.Row="0" Grid.Column="2" Content="Browse" Icon="Folder24" Margin="5" Click="ButtonSelectFile_Click"/>

                <Label Content="Destination Folder:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <ui:TextBox x:Name="TextBoxDestination" Grid.Row="1" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>
                <ui:Button x:Name="ButtonSelectDestination" Grid.Row="1" Grid.Column="2" Content="Browse" Icon="Folder24" Margin="5" Click="ButtonSelectDestination_Click"/>

                <CheckBox x:Name="CheckBoxCleanup" Grid.Row="2" Grid.Column="1" Content="Remove finished items from report" Margin="5" VerticalAlignment="Center" IsChecked="True"/>
            </Grid>
        </GroupBox>

        <!-- Connection Section -->
        <GroupBox Header="Connection" Grid.Row="2" Margin="0,0,0,10">
             <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="Auto" MinWidth="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Content="Download Website:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <ui:TextBox x:Name="TextBoxWebsite" Grid.Row="0" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

                <Label Content="Username:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <ui:TextBox x:Name="TextBoxUsername" Grid.Row="1" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

                <Label Content="Password:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                <ui:PasswordBox x:Name="PBoxPassword" Grid.Row="2" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <ui:Button x:Name="ButtonDownload" Content="Start Download" Icon="ArrowDownload24" Width="160" Margin="5" Click="ButtonDownload_Click" IsDefault="True" Appearance="Primary"/>
            <ui:Button x:Name="ButtonAbort" Content="Stop" Icon="Dismiss24" Width="120" Margin="5" Click="ButtonAbort_OnClick" IsEnabled="False" IsCancel="True" Appearance="Danger"/>
        </StackPanel>

        <!-- Per-file status -->
        <GroupBox Header="Current Downloads" Grid.Row="4" Margin="0,0,0,10" Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid Margin="10">
                <ui:ListView x:Name="LvDownloads"
                             ItemsSource="{Binding DownloadProgressCollection}"
                             BorderThickness="1"
                             BorderBrush="#33000000"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.View>
                        <ui:GridView>
                            <ui:GridViewColumn Header="File" Width="310" DisplayMemberBinding="{Binding FileName}"/>
                            <ui:GridViewColumn Header="Progress" Width="220">
                                <ui:GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <ProgressBar Height="12" Minimum="0" Maximum="100" Value="{Binding ProgressPercent, Mode=OneWay}"/>
                                            <TextBlock Foreground="Gray" Text="{Binding ProgressText}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ui:GridViewColumn.CellTemplate>
                            </ui:GridViewColumn>
                            <ui:GridViewColumn Header="Speed" Width="110" DisplayMemberBinding="{Binding SpeedText}"/>
                        </ui:GridView>
                    </ListView.View>
                </ui:ListView>
            </Grid>
        </GroupBox>

        <ProgressBar x:Name="ProgressBarDownload" Grid.Row="5" Height="4" Minimum="0" Maximum="100"/> 
    </Grid>
</ui:FluentWindow>
